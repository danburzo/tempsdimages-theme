{% extends 'single/single-editie.twig' %}

{% block editie_content %}

	{% if locuri | length %}
		{% for loc in locuri %}
			{% 
				include 'tease/tease-loc.twig' with {
					'post': loc
				}
			%}
		{% endfor %}

		<h3>{{__("Hartă", "TDI")}}</h3>

		{% include 'components/map.twig' %}
		
	{% else %}
		<p>{{__("Nu există încă locuri adăugate pentru această ediție", "TDI")}}</p>
	{% endif %}
{% endblock %}