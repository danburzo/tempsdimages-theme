{% extends "base.twig" %}

{% block content %}
	<h2>{{foo}}</h2>
	<p>{{qux}}</p>
	{% for post in posts %}
		{% include ['tease/tease-'~post.post_type~'.twig', 'tease/tease.twig'] %}
	{% endfor %}
{% endblock %}